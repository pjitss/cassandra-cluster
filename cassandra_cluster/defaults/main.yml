# defaults/main.yml
num_cassandra_nodes: "{{ NUM_CASSANDRA_NODES }}"
cassandra_cluster_name: "TestCluster"
cassandra_max_heap_size: "1024M"
cassandra_heap_newsize: "256M"
cassandra_cpus: "{{ CASSANDRA_CPUS }}"
cassandra_ram: "{{ CASSANDRA_RAM }}"
num_spark_nodes: "{{ NUM_SPARK_NODES }}"
spark_worker_cores: "0.5"
spark_worker_memory: "1024M"
spark_cpus: "{{ SPARK_CPUS }}"
spark_ram: "{{ SPARK_RAM }}"